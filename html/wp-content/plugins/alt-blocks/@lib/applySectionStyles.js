function applySectionStyles(attributes) {
	const SECTION_STYLES = {
		padding: `${PADDING?.top} ${PADDING?.right} ${PADDING?.bottom} ${PADDING?.left}`,
	};

	return SECTION_STYLES;
}

export default applySectionStyles;
